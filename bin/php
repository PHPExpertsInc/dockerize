#!/bin/bash

# Use 'echo pid1' to consume process id 1 so we can send kill signals to php (which will be process id 2)
COMMAND="echo pid1 > /dev/null && php $@"

docker run -it -v $(pwd):/data --rm chekote/php "${COMMAND}"
